//! Modèle de données du coffre (implémenté au lot P0.3).
//!
//! Entités mappées sur le CRDT : identifiants (credentials), profils, TOTP,
//! champs personnalisés. Chaque entrée = map `champ -> LwwRegister`.

// TODO(P0.3): types Vault, Entry, EntryId ; mapping vers le CRDT.
